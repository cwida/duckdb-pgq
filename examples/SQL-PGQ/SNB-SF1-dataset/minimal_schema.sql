CREATE OR REPLACE TABLE "Person_knows_Person"("creationDate" TIMESTAMP WITH TIME ZONE NOT NULL, "Person1id" BIGINT, "Person2id" BIGINT, PRIMARY KEY("Person1id", "Person2id"));
CREATE OR REPLACE TABLE "Person"("creationDate" TIMESTAMP WITH TIME ZONE NOT NULL, id BIGINT PRIMARY KEY, "firstName" VARCHAR NOT NULL, "lastName" VARCHAR NOT NULL, gender VARCHAR NOT NULL, birthday DATE NOT NULL, "locationIP" VARCHAR NOT NULL, "browserUsed" VARCHAR NOT NULL, "LocationCityId" BIGINT NOT NULL, speaks VARCHAR NOT NULL, email VARCHAR NOT NULL);

