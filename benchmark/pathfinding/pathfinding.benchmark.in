# name: ${FILE_PATH}
# description: ${DESCRIPTION}
# group: [pathfinding]

name ${QUERY_NAME}
group pathfinding

require duckpgq

load benchmark/pathfinding/load.sql

run benchmark/pathfinding/queries/${QUERY_NAME}.sql